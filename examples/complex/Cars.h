#ifndef __CARS_H__
#define __CARS_H__

// This an interface header: it does not contain any declarations, but is used as a simple interface
// to a set of headers. IncludeChecker will by default always mark such a file as unused.
// But if this file is marked as an interface header, IncludeChecker will check against all includes that
// are in this file, all includes in those included files, etc. See includechecker-complex.xml.

#include "Car.h"
#include "HybridCar.h"
#include "SuperCar.h"

#endif // __CARS_H__
